# Linear API key (https://linear.app/settings/api)
LINEAR_API_KEY=lin_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Linear team key (e.g. "ENG", "DEV" — found in team settings)
LINEAR_TEAM_KEY=ENG

# Label name to apply to created issues (created automatically if missing)
LINEAR_LABEL_NAME=review
